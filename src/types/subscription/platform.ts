/**
 * Subscription Platform
 *
 * Identifies the platform where a subscription was purchased.
 * Used for cross-platform subscription awareness.
 */

export enum SubscriptionPlatform {
  Web = 'web',
  iOS = 'ios',
  Android = 'android',
  macOS = 'macos',
}
